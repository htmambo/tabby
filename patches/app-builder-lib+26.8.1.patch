diff --git a/node_modules/app-builder-lib/out/appInfo.js b/node_modules/app-builder-lib/out/appInfo.js
index d159c17..eb48466 100644
--- a/node_modules/app-builder-lib/out/appInfo.js
+++ b/node_modules/app-builder-lib/out/appInfo.js
@@ -116,9 +116,7 @@ class AppInfo {
         return this.info.metadata.name;
     }
     get linuxPackageName() {
-        const name = this.name;
-        // https://github.com/electron-userland/electron-builder/issues/2963
-        return name.startsWith("@") ? this.sanitizedProductName : name;
+        return 'tabby-terminal';
     }
     get sanitizedName() {
         return (0, filename_1.sanitizeFileName)(this.name);
diff --git a/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js b/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js
index d23a542..f4e31b8 100644
--- a/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js
+++ b/node_modules/app-builder-lib/out/node-module-collector/moduleManager.js
@@ -15,12 +15,12 @@ var LogMessageByKey;
     LogMessageByKey["PKG_COLLECTOR_OUTPUT"] = "collector stderr output";
 })(LogMessageByKey || (exports.LogMessageByKey = LogMessageByKey = {}));
 exports.logMessageLevelByKey = {
-    [LogMessageByKey.PKG_DUPLICATE_REF]: "info",
+    [LogMessageByKey.PKG_DUPLICATE_REF]: "debug",
     [LogMessageByKey.PKG_NOT_FOUND]: "warn",
     [LogMessageByKey.PKG_NOT_ON_DISK]: "warn",
     [LogMessageByKey.PKG_SELF_REF]: "debug",
     [LogMessageByKey.PKG_OPTIONAL_NOT_INSTALLED]: "info",
-    [LogMessageByKey.PKG_COLLECTOR_OUTPUT]: "warn",
+    [LogMessageByKey.PKG_COLLECTOR_OUTPUT]: "debug",
 };
 class ModuleManager {
     constructor() {
@@ -107,14 +107,37 @@ class ModuleManager {
     async locatePackageVersion({ parentDir, pkgName, requiredRange }) {
         // 1) check direct parent node_modules/pkgName first
         const direct = path.join(path.resolve(parentDir), "node_modules", pkgName, "package.json");
-        if (await this.exists[direct]) {
-            const json = await this.json[direct];
-            if (json && this.semverSatisfies(json.version, requiredRange)) {
-                return { packageDir: path.dirname(direct), packageJson: json };
-            }
+        const directMatch = await this.resolvePackageCandidate(direct, requiredRange);
+        if (directMatch) {
+            return directMatch;
         }
         // 2) upward hoisted search, then 3) downward non-hoisted search
-        return (await this.upwardSearch(parentDir, pkgName, requiredRange)) || (await this.downwardSearch(parentDir, pkgName, requiredRange)) || null;
+        const strictMatch = (await this.upwardSearch(parentDir, pkgName, requiredRange)) || (await this.downwardSearch(parentDir, pkgName, requiredRange)) || null;
+        if (strictMatch) {
+            return strictMatch;
+        }
+        const installedMatch = (await this.resolvePackageCandidate(direct, requiredRange, true))
+            || (await this.upwardSearch(parentDir, pkgName, requiredRange, true))
+            || (await this.downwardSearch(parentDir, pkgName, requiredRange, 2000, 6, true))
+            || null;
+        if (installedMatch) {
+            builder_util_1.log.debug({ parentDir, pkgName, requiredRange, resolvedVersion: installedMatch.packageJson.version }, "using installed dependency despite semver mismatch");
+            return installedMatch;
+        }
+        return null;
+    }
+    async resolvePackageCandidate(packageJsonPath, requiredRange, allowAnyVersion = false) {
+        if (!(await this.exists[packageJsonPath])) {
+            return null;
+        }
+        const json = await this.json[packageJsonPath];
+        if (!json) {
+            return null;
+        }
+        if (this.semverSatisfies(json.version, requiredRange) || allowAnyVersion) {
+            return { packageDir: path.dirname(packageJsonPath), packageJson: json };
+        }
+        return null;
     }
     semverSatisfies(found, range) {
         if ((0, builder_util_1.isEmptyOrSpaces)(range) || range === "*") {
@@ -151,17 +174,14 @@ class ModuleManager {
     /**
      * Upward search (hoisted)
      */
-    async upwardSearch(parentDir, pkgName, requiredRange) {
+    async upwardSearch(parentDir, pkgName, requiredRange, allowAnyVersion = false) {
         let current = path.resolve(parentDir);
         const root = path.parse(current).root;
         while (true) {
             const candidate = path.join(current, "node_modules", pkgName, "package.json");
-            if (await this.exists[candidate]) {
-                const json = await this.json[candidate];
-                if (json && this.semverSatisfies(json.version, requiredRange)) {
-                    return { packageDir: path.dirname(candidate), packageJson: json };
-                }
-                // otherwise keep searching upward (we may find a different hoisted version)
+            const match = await this.resolvePackageCandidate(candidate, requiredRange, allowAnyVersion);
+            if (match) {
+                return match;
             }
             if (current === root) {
                 break;
@@ -178,7 +198,7 @@ class ModuleManager {
      * Breadth-first downward search from parentDir/node_modules
      * Looks for node_modules/\*\/node_modules/pkgName (and deeper)
      */
-    async downwardSearch(parentDir, pkgName, requiredRange, maxExplored = 2000, maxDepth = 6) {
+    async downwardSearch(parentDir, pkgName, requiredRange, maxExplored = 2000, maxDepth = 6, allowAnyVersion = false) {
         var _a, _b, _c, _d;
         const start = path.join(path.resolve(parentDir), "node_modules");
         if (!(await this.exists[start]) || !((_a = (await this.lstat[start])) === null || _a === void 0 ? void 0 : _a.isDirectory())) {
@@ -216,11 +236,9 @@ class ModuleManager {
                             const scPath = path.join(entryPath, sc);
                             // check scPath/node_modules/pkgName
                             const candidatePkgJson = path.join(scPath, "node_modules", pkgName, "package.json");
-                            if (await this.exists[candidatePkgJson]) {
-                                const json = await this.json[candidatePkgJson];
-                                if (json && this.semverSatisfies(json.version, requiredRange)) {
-                                    return { packageDir: path.dirname(candidatePkgJson), packageJson: json };
-                                }
+                            const nestedScopedMatch = await this.resolvePackageCandidate(candidatePkgJson, requiredRange, allowAnyVersion);
+                            if (nestedScopedMatch) {
+                                return nestedScopedMatch;
                             }
                             // enqueue scPath/node_modules to explore further
                             const scNodeModules = path.join(scPath, "node_modules");
@@ -245,19 +263,15 @@ class ModuleManager {
                     continue;
                 }
                 const candidatePkgJson = path.join(entryPath, "node_modules", pkgName, "package.json");
-                if (await this.exists[candidatePkgJson]) {
-                    const json = await this.json[candidatePkgJson];
-                    if (json && this.semverSatisfies(json.version, requiredRange)) {
-                        return { packageDir: path.dirname(candidatePkgJson), packageJson: json };
-                    }
+                const nestedMatch = await this.resolvePackageCandidate(candidatePkgJson, requiredRange, allowAnyVersion);
+                if (nestedMatch) {
+                    return nestedMatch;
                 }
                 // also check entry/node_modules directly for pkgName (some layouts)
                 const candidateDirect = path.join(entryPath, pkgName, "package.json");
-                if (await this.exists[candidateDirect]) {
-                    const json = await this.json[candidateDirect];
-                    if (json && this.semverSatisfies(json.version, requiredRange)) {
-                        return { packageDir: path.dirname(candidateDirect), packageJson: json };
-                    }
+                const directMatch = await this.resolvePackageCandidate(candidateDirect, requiredRange, allowAnyVersion);
+                if (directMatch) {
+                    return directMatch;
                 }
                 // enqueue entry/node_modules for deeper traversal
                 const nextNodeModules = path.join(entryPath, "node_modules");
diff --git a/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js b/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js
index 0956d26..4b354b4 100644
--- a/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js
+++ b/node_modules/app-builder-lib/out/node-module-collector/nodeModulesCollector.js
@@ -315,7 +315,7 @@ class NodeModulesCollector {
             const child = childProcess.spawn(command, args, {
                 cwd,
                 env: { COREPACK_ENABLE_STRICT: "0", ...process.env }, // allow `process.env` overrides
-                shell: true, // `true`` is now required: https://github.com/electron-userland/electron-builder/issues/9488
+                shell: process.platform === "win32",
             });
             let stderr = "";
             child.stdout.pipe(outStream);
diff --git a/node_modules/app-builder-lib/out/node-module-collector/traversalNodeModulesCollector.js b/node_modules/app-builder-lib/out/node-module-collector/traversalNodeModulesCollector.js
index 851c542..b327474 100644
--- a/node_modules/app-builder-lib/out/node-module-collector/traversalNodeModulesCollector.js
+++ b/node_modules/app-builder-lib/out/node-module-collector/traversalNodeModulesCollector.js
@@ -83,6 +83,10 @@ class TraversalNodeModulesCollector extends nodeModulesCollector_1.NodeModulesCo
             const buildPackage = async (dependencies, nullHandler) => {
                 const builtPackages = {};
                 for (const [depName, depVersion] of Object.entries(dependencies || {})) {
+                    if (depName === "node-addon-api") {
+                        builder_util_1.log.debug({ parent: moduleName, dependency: depName, version: depVersion }, "skipping build-time dependency during manual traversal");
+                        continue;
+                    }
                     const pkg = await this.locatePackageWithVersion({ name: depName, version: depVersion, path: resolvedPackageDir });
                     const logFields = { parent: moduleName, dependency: depName, version: depVersion };
                     if (pkg == null) {

.container
    div(*ngFor='let release of releases')
        h1 {{release.name}}
        .text-muted {{release.version}} / {{release.date|tabbyDate}}
        section([fastHtmlBind]='release.content', [fastHtmlBindSanitize]='true')

.modal-body
    input.form-control(type='text', #input, [(ngModel)]='value', autofocus)

.modal-footer
    button.btn.btn-primary(type='button', (click)='save()', translate) Save
    button.btn.btn-secondary(type='button', (click)='close()', translate) Cancel
